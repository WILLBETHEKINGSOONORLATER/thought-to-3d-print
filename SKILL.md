---
name: thought-to-3d-print
description: 想啥印啥 —— 把「一句话 → 可打开的 3D 模型」一次走通，最后在 Bambu Studio（拓竹切片软件）里打开。有两条路径：**路径 A 造型件**用「文生图 → 图生3D」（摆件、手办、造型挂件）；**路径 B 精确配合件**用「参数化建模」（外壳、收纳盒、任何要塞东西进去或需要开孔的件）。适用于想把 AI 生成的或算出来的 3D 模型直接送到拓竹 App 里查看、调参、打印的场景。规则：每产生一个可看的新产出（参考图 / 尺寸参数表 / 3D 模型 / 待打开的文件）都要先告知用户、等用户点头再继续，不许一路自动到底。边界明确：交付到「模型在拓竹 App 里成功打开」为止，不做切片。用户不需要安装任何东西（路径 A 零依赖；路径 B 所需环境由 AI 自己准备）。Turn a description into a 3D model opened in Bambu Studio — either via text-to-image → image-to-3D for sculptural parts, or via parametric modeling for precise fitting parts like enclosures — notifying the user at each new deliverable.
agent_created: true
---

# 想啥印啥 · 从一句话到拓竹 3D 模型

输入一句描述，输出一个能在 Bambu Studio（拓竹的桌面端切片软件）里直接打开的 3D 模型。

**两条路径，先选路再动手：**

| | 路径 A · 造型件 | 路径 B · 精确配合件 |
|---|---|---|
| 用户要的是 | **"像什么"** | **"装得下什么、哪里透气"** |
| 典型物件 | 摆件、手办、造型挂件、公仔 | 外壳、收纳盒、支架、任何"要塞东西进去"的件 |
| 做法 | 文生图 → 图生3D | 参数化建模（写数字 → 算几何） |
| 步骤 | 第 1 ~ 5 步 | 第 B1 ~ B6 步（见下文，细节必读 `references/parametric-modeling.md`） |

**判断标准只有一句：用户要的是外观，还是配合？** 拿不准就问一句——"**这个东西是要塞别的东西进去，还是只是好看？**"

> ⚠️ 路径 B 的完整流程、参数规则、声学设计和踩坑清单都在 **`references/parametric-modeling.md`**。走路径 B 之前必须读完它，别凭印象建模。

**共同的终点**：模型在拓竹 App 里成功打开。

**共同不做的事**：**切片（slicing）**。切片（把模型切成打印机认识的指令）交给用户在 App 里完成——那部分操作在图形界面里更顺手。

---


## ⚠️ 贯穿全程的一条规则：每个「质的提升」都要告知用户

这条链路上每产生一个**新的、可看的产出**，就是一次质的提升。每到这时必须停下来，做三件事：

1. **展示** —— 把新产出直接给用户看（图片 / 模型文件 / 尺寸数据），不要只用文字描述
2. **告知** —— 说清这是什么、下一步要做什么
3. **等答复** —— 用户点头才继续

**不许在同一条回复里既展示又继续往下做。**

本链路有三个这样的节点：

| 节点 | 新产出 | 展示什么 | 一句话告知 |
|---|---|---|---|
| **1** | 参考图生成完 | 图片 | 「参考图出来了，可以吗？满意我就接着做 3D。」 |
| **2** | 3D 模型完成 | 模型文件 / 预览图 / 尺寸 | 「3D 模型出来了，形状对吗？可以我就去摆正、准备打开。」 |
| **3** | 准备打开前 | 尺寸 + 对照机型表是否装得下 | 「模型是 115×92×117 mm，你的 H2C 装得下。现在在拓竹 App 里打开吗？」 |

**路径 B 的节点位置不同**（因为中间不是"生成"而是"算"）：

| 节点 | 新产出 | 展示什么 | 一句话告知 |
|---|---|---|---|
| **B-1** | 关键尺寸参数表 | 尺寸表（还没建模） | 「我按这些数做：内腔 Ø24…有要改的吗？」 |
| **B-2** | 模型算完（＝节点 2） | 模型文件 + 自检数值 | 「两个零件都出来了、都是封闭实体，尺寸如下，对吗？」 |
| **B-3** | 准备打开前（＝节点 3） | 尺寸 + 装得下吗 | 同节点 3 |

**节点 B-1 是路径 A 没有的一步**——在动代码之前先让用户确认尺寸，能省掉一次白干。

### 只说事实，不做归因

用户说「不对」时，**如实转述、问清具体哪里不符合预期即可**，不要替用户断定原因是哪一步造成的——同一个现象可能有多种成因，判断权在用户。

---

## ⚠️ 前置条件只有一条

**用户的电脑上只需要装了拓竹 App（Bambu Studio）。**

**绝对不要要求用户安装 Python 库、pip、trimesh 等任何东西。** 面向的是不懂技术的用户，任何"打开终端粘贴命令"的要求都会劝退他们。

| 路径 | 用户要装什么 | AI 要准备什么 |
|---|---|---|
| A · 造型件 | **什么都不用装** | 无（摆正脚本是纯标准库写的） |
| B · 配合件 | **什么都不用装** | 跑一次 `scripts/setup_env.py` 备好几何库 |

**路径 B 的 Python 环境是 AI 这一侧的事。** 用户拖文件夹、说需求，流程一个字都不变 —— 这条界线绝不能越过去。

---

## 第 0 步：先确认设备（别跳）

**不同拓竹机型的打印体积差别很大，这一步决定后面模型是否超尺寸。**

只需要问一件事：**你的打印机是什么型号？**（喷嘴直径默认 0.4mm，用户不懂就别问）

### 拓竹机型打印体积速查

| 机型 | 打印体积（宽×深×高） |
|---|---|
| A1 mini | 180×180×180 mm |
| A1 / P1P / P1S | 256×256×256 mm |
| X1 / X1C / X1E | 256×256×256 mm |
| H2S | 340×320×340 mm |
| H2D | 350×320×320 mm |
| H2C | 330×320×320 mm |

> 机型迭代较快，以拓竹官网最新规格为准。

模型生成后，**拿尺寸对照这张表**，装不下就提前告诉用户。

---

## 路径 B：精确配合件（第 B1 ~ B6 步）

> **只做"要塞东西进去 / 要开孔 / 尺寸必须准"的件。** 细节规则见 `references/parametric-modeling.md`，本节只给流程骨架。

### B1 · 量尺寸（先问，别猜）

必须拿到：物件的**直径/长宽**、**厚度**、**哪里有孔哪里要出声**、**怎么充电（要不要能取出来）**、**外壳多大什么形状**、**有没有凸起物（磁吸垫片、按键）及其张开后的厚度**。

> ⚠️ 不要凭印象填。查不到就让用户量；量不了就按最保守估并**明确告知这是估的**。
> 麦克风位置不知道 → **全方位开孔**（侧面四个方向都敞、顶底都开窗）。

### B2 · 让用户选开合方式

物件要取出来充电，所以壳必须能开。**默认推荐一体卡扣**（无额外零件，打印完直接用）；磁铁吸合更顺手但要买配件；螺丝最结实但件要厚。

### 🛑 节点 B-1：参数表先给用户确认

动代码之前，把要用的关键尺寸列成表发过去（内腔直径、净高、敞开率、窗口尺寸、外形尺寸、开合方式），等答复。**这一步能省掉一次白干。**

### B3 · 备环境（AI 侧一次性）

```bash
python3 <skill目录>/scripts/setup_env.py     # 跑完会打印 venv 的 python 绝对路径
```

**用户完全不参与这一步。**

### B4 · 改参数 → 干跑 → 出件

打开 `scripts/param_shell.py`，改最上面的【参数区】（顶部有中英文注释说明每个参数干嘛的）：

```bash
# 先干跑，只打印尺寸自检、不写文件 —— 数值不对就别出件
<venv>/bin/python <skill目录>/scripts/param_shell.py --report

# 数值 OK 再出件
<venv>/bin/python <skill目录>/scripts/param_shell.py --out ./out
```

产出：`body.stl`（下盆）、`lid.stl`（顶盖）、`shell.3mf`（两件合装，一次拖进拓竹 App）。

脚本自带参数合法性检查（圆角被削穿、沉台挖穿、内腔装不下等）会**在跑几何前直接拦下**，报错就改参数重跑。

### B5 · 自检（必做，每条都要过）

`--report` 会打印：**每个零件是否水密**（watertight，模型完全封闭无破洞）、**体积是否为正**、内腔直径/净高是否够、**有效孔长 ≤0.8mm**、**侧面敞开率 ≥40%**、卡扣压入量 ≥0.2mm。

不水密或体积为负 → **别交付**，先查参数。

### B6 · 把模型交给用户看（🛑 节点 2）

三个文件 + 自检数值一起给用户，然后停下等答复。**不要在同一条回复里既给文件又自己打开 App。**

用户认可 → 跳到 **第 5 步**（摆正那步一般不用跑，脚本出的模型本来就居中贴地）。

---

> **━━ 路径 A · 造型件（第 1 ~ 5 步）━━**
> 以下步骤只服务于"好看"的件。配合件请用上面的路径 B，直接跳到第 5 步。

## 第 1 步：文生图 —— 出「3D 参考图」，不是插画

这是整条链路里**最容易被做错、又最影响最终质量**的一步。

图生3D 是从**像素**推断**几何**的，喂进去的图必须让几何信息尽可能清晰：

| 要 | 不要 |
|---|---|
| 纯白 / 纯净背景 | 复杂场景、环境光效 |
| 单主体、居中、完整不裁切 | 多物体、相互遮挡 |
| 正面或标准三视角 | 大透视、极端角度 |
| 正交视角（无透视变形） | 广角畸变 |
| 轮廓清晰、结构分明 | 大面积模糊、半透明 |
| 无文字、无水印、无 logo | 任何文字/水印/签名 |

**核心判断**：问自己"这张图能不能让人一眼捏出泥塑"。不能，就重画。

### 🛑 节点 1：参考图出来，先给用户看

把图片**直接展示给用户**，说清这是什么、下一步做什么，然后等答复：

> 「参考图出来了，可以吗？满意我就接着做 3D。」

- 用户满意 → 进入第 2 步
- 用户不满意 → **问清具体哪里不符合预期**，重画后再展示，直到用户点头

不要自己判断「差不多行了」就往下走。

---

## 第 2 步：去水印 —— 别省这一步

图上的水印、签名、logo，会被当成**真实几何**融进 3D 模型的表面，生成之后基本清不掉。

用图像编辑能力（修补 / 生成式填充均可）处理干净，再进下一步。

---

## 第 3 步：图生3D —— 关键在参数选择

用可用的 image-to-3D 服务。以下是**通用的参数原则**，各家命名略有差异，概念一致：

| 参数 | 该选什么 | 为什么 |
|---|---|---|
| **生成类型** | **白模 / Geometry**（纯几何，无纹理） | 实体打印只消耗形状。PBR（物理基础渲染材质，控制金属感、粗糙度等表面外观）对打印**完全无用**，只会拖慢生成、虚高面数、撑大文件 |
| **输出格式** | **STL** | 拓竹 App 能直接打开，一步到位无需转换 |
| **面数** | 默认值即可 | 打印精度受喷嘴限制（0.4mm 喷嘴下，几十万面已远超需要） |

**图片过大时**先压缩再上传（多数服务有体积上限）。

> 服务选型参考：腾讯混元 3D、Meshy、Tripo、Rodin 等。核心看是否支持**白模输出**和 **STL 导出**。

生成通常需要 1~5 分钟。

### 🛑 节点 2：3D 模型出来，先给用户看

把产出**展示给用户**（模型文件路径 / 预览图 / 尺寸数据），说清下一步，然后等答复：

> 「3D 模型出来了，形状对吗？可以我就去摆正、准备在拓竹 App 里打开。」

- 用户认可 → 进入第 4 步
- 用户不认可 → **问清具体是哪里不对**。不要自己猜测成因，也不要擅自重复生成——按用户说的来。

---

## 第 4 步：摆正（用下面的脚本，不要装库）

> 这一步只对**路径 A 生成出来的模型**有必要——AI 生成模型的坐标原点是随机的。
> **路径 B 用脚本算出来的模型本来就 XY 居中、底面贴地，不用跑这一步。**

AI 生成的模型原点位置是随机的，经常有一半在"地面"以下。跑一遍脚本就正了。

**执行方式**：把下面这段代码原样写到临时文件（例如 `/tmp/stl_fix.py`），然后运行（本 skill 目录下 `scripts/stl_fix.py` 有同一份文件，直接复制更省事）：

```bash
python3 /tmp/stl_fix.py 模型.stl
```

Windows 上用 `python` 代替 `python3`。如果系统没有 Python 命令，跳过本步即可——**模型不摆正也能在拓竹 App 里正常打开，用户手动拖一下就行，不要为此让用户去装 Python。**

### 脚本代码（纯标准库，零依赖）

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""把 STL 摆正：XY 居中，最低点贴地(z=0)。纯标准库，无需安装任何库。
用法: python3 stl_fix.py 模型.stl [输出.stl]"""
import os, struct, sys

HEADER_SIZE, COUNT_SIZE, TRIANGLE_SIZE = 80, 4, 50


def read_binary_stl(path):
    with open(path, 'rb') as f:
        raw = f.read()
    if len(raw) < HEADER_SIZE + COUNT_SIZE:
        raise ValueError('文件太小，不像有效的 STL')
    header = raw[:HEADER_SIZE]
    count = struct.unpack('<I', raw[HEADER_SIZE:HEADER_SIZE + COUNT_SIZE])[0]
    if len(raw) != HEADER_SIZE + COUNT_SIZE + count * TRIANGLE_SIZE:
        raise ValueError('不是二进制 STL（可能是 ASCII 格式），请导出时选「二进制/Binary STL」')
    return header, count, raw[HEADER_SIZE + COUNT_SIZE:]


def bounds(body):
    lo = [float('inf')] * 3
    hi = [float('-inf')] * 3
    for v in struct.iter_unpack('<12fH', body):
        for i in (3, 6, 9):                      # 三个顶点
            for a in range(3):
                c = v[i + a]
                if c < lo[a]: lo[a] = c
                if c > hi[a]: hi[a] = c
    return lo, hi


def shift(body, d):
    out = bytearray()
    for v in struct.iter_unpack('<12fH', body):
        out += struct.pack(
            '<12fH',
            v[0], v[1], v[2],                                    # 法向量不动
            v[3] + d[0], v[4] + d[1], v[5] + d[2],
            v[6] + d[0], v[7] + d[1], v[8] + d[2],
            v[9] + d[0], v[10] + d[1], v[11] + d[2],
            v[12],
        )
    return bytes(out)


def main():
    if len(sys.argv) < 2:
        print(__doc__); sys.exit(1)
    src = sys.argv[1]
    base, ext = os.path.splitext(src)
    dst = sys.argv[2] if len(sys.argv) > 2 else base + '_onbed' + (ext or '.stl')

    header, count, body = read_binary_stl(src)
    lo, hi = bounds(body)
    size = [hi[i] - lo[i] for i in range(3)]
    print('原始尺寸：%.1f x %.1f x %.1f mm' % tuple(size))
    print('三角面数：%d' % count)

    d = (-(lo[0] + hi[0]) / 2.0, -(lo[1] + hi[1]) / 2.0, -lo[2])
    print('平移量：dx=%.2f dy=%.2f dz=%.2f' % d)

    with open(dst, 'wb') as f:
        f.write(header)
        f.write(struct.pack('<I', count))
        f.write(shift(body, d))
    print('已保存：' + dst)
    print('现在模型 %.1f x %.1f x %.1f mm，居中立在打印板上。' % tuple(size))


if __name__ == '__main__':
    main()
```

**脚本会输出模型的长宽高**——顺手就拿这个数字去对照第 0 步的机型表，不用再单独查尺寸。

---

## 第 5 步：在拓竹 App 里打开

### 支持格式

拓竹 App 可直接打开：**STL / AMF / 3MF / GCODE**

STL 是图生3D 服务能直接输出的格式，一步到位，**默认就用它**。

### 🛑 节点 3：打开之前，先告知

打开前，把**尺寸**和**对照第 0 步机型表能否装下**的事实告诉用户，然后等答复：

> 「模型是 115×92×117 mm，你的 H2C（330×320×320 mm）装得下。现在在拓竹 App 里打开吗？」

- 用户同意 → 打开
- 用户想调整 → 按用户说的做
- 用户说先等等 → 只交付文件路径，不打开

打开之后，告知文件路径和「已在 Bambu Studio 里打开」，让用户确认加载正常。

### 打开命令

**macOS**：

```bash
open -g -a BambuStudio "/绝对路径/模型.stl"
```

**Windows**：

```powershell
& "C:\Program Files\Bambu Studio\bambu-studio.exe" "C:\路径\模型.stl"
```

> macOS 的 `-g` 表示后台打开、不抢焦点，不会打断用户当前操作。
> 若提示找不到应用，改用 `open -a "Bambu Studio"`。

---

## 交付时顺手给用户的切片建议

模型打开后用户要自己切片，这几组参数有实测依据：

| 参数 | 建议值 |
|---|---|
| 层高 | 0.20mm Standard |
| 填充 | 10%（摆件类）/ 15~20%（受力件） |
| **支撑** | **树状（自动）** |

**支撑选型实测对比**（同一模型，0.2mm 层高）：

| 方案 | 耗时 |
|---|---|
| **树状支撑** | **2h37m** ← 最优 |
| 无支撑 | 2h39m（有塌陷风险） |
| 普通支撑 | 3h40m |

**材料选择**：

| 材料 | 适用 |
|---|---|
| PLA | 摆件、展示件（脆、耐热 55~60℃，别放车里） |
| PETG | 需要韧性和耐热的场合（耐热 70~80℃） |

---

## 关于模型质量：不要过度加工

实测结论：**面数、开放边数（破洞）、零件数（碎块）都不影响在拓竹 App 里打开和使用。**

验证数据：一个含 **418 条开放边、32 个独立碎块**的网格文件，在拓竹 App 里完全正常。

**不要因为这些指标难看就反复修模型、重新生成或做网格清理**——那是纯粹浪费用户的时间和 AI 额度。模型能打开、尺寸对、形状是用户要的，就够了。

> **这条只针对路径 A 生成出来的模型。**
> 路径 B 用脚本算出来的件，脚本本身就会保证水密、体积为正。**如果路径 B 报出"不水密"，那是参数有问题，不是"不用管"**——回去改参数重跑。

---

## 常见问题

**Q：模型打开是歪的 / 陷进板子里？**
A：跑第 4 步的脚本。跑不了也没关系，用户在 App 里拖一下即可。

**Q：模型显示红色 / 提示需要修复？**
A：正常。在 App 里点修复就行，不影响打印。

**Q：生成的模型糊成一团？**
A：多半是第 1 步的输入图问题（背景杂乱、角度刁钻、主体被遮挡）。回去重画参考图，比在 3D 阶段补救有效得多。

**Q：模型太大了？**
A：对照第 0 步的机型表。装不下就在拓竹 App 里缩放，或在图生3D 时减小尺寸参数。

**Q：用户想要能直接打印的文件？**
A：说明切片不在本 skill 范围内，引导用户把打开后的模型在 Bambu Studio 里自行切片——界面上点几下就行。

**Q：用户要的是"壳 / 盒子 / 支架"，图生3D 出来的装不进东西？**
A：路径选错了，走 **路径 B**。图生3D 产出的是实心或薄壳，没有内腔、没有真通孔、尺寸也不可控。见 `references/parametric-modeling.md`。

**Q：卡扣压不进去 / 压进去就滑出来？**
A：调 `SNAP_R`（0.6~0.8mm 合适）和 `CLA`（滑动间隙 0.2mm）。压入内缩量 = `SNAP_R − CLA`，低于 0.2mm 钩不住、高于 0.8mm 压不进。

**Q：用户说"放进去收不到声 / 声音闷"？**
A：三个方向查——① 侧面敞开率够不够（要 ≥40%）；② **有效孔长**是不是太长（`PLATE − CB_D` 要 ≤0.8mm，孔道一长高频就被削掉）；③ 有没有把内腔封成密闭气室。

**Q：用户问"要不要装什么软件"？**
A：**不用。** 只需要装拓竹 App。路径 B 的 Python 环境由 AI 这边准备，用户不参与。

---

## 最后一句

**先选路：造型件走 A（生成），配合件走 B（算）。**

**路径 A 里真正需要动脑的只有两处：第 1 步的参考图质量，和第 3 步的白模参数选择。**

**路径 B 里真正需要动脑的是两处：B1 把尺寸问准，B5 把自检数值过一遍。**

**自动化到哪一步都行，但每产生一个可看的新产出，都要先告知用户、等用户点头再往下。**
